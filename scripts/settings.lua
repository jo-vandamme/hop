print("Loading settings")

options = {
    frame_width = 800,
    frame_height = 600,
    spp = 10,
    adaptive_spp = 100,
    adaptive_threshold = 1.0,
    adaptive_exponent = 2,
    preview_spp = 2,
}
